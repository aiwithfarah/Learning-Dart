// Task: Inside the main() function:
// Create a variable to store a user's username (e.g., "DartMaster").
// Create a variable to store their level (e.g., 5).
// Create a variable to store their score (e.g., 95.5).
// Create a variable to check if they are active (true/false).
// Print all four variables to the console.

void main() {
  String username = 'DartMaster';
  int level = 5;
  double score = 95.5;
  bool active = false;

  print(username);
  print(level);
  print(score);
  print(active);
}
